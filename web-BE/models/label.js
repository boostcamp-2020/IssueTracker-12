// const connection = require('../config/db_connection');
// const query = require('../util/query');

const labelModel = {
  insert: () => true,
  select: () => true,
  update: () => true,
  delete: () => true,
};
